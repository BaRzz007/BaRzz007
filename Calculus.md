Understood — I will provide:

### ✅ **Two complete Markdown source files**

* **Full Version (Style B – modern study notes)**
* **Condensed Version (Style A – classic textbook)**

Both will be written so that you can **export them to PDF** using any Markdown-to-PDF tool (Typora, Obsidian, VS Code Markdown PDF, Pandoc, etc.).

### ⚠️ Note

ChatGPT cannot create binary PDF files directly in this environment — but the Markdown I provide is fully styled and structured for clean PDF rendering.

---

# 📄 **PDF 1 — FULL VERSION (Style B: Modern Study Notes)**

### **Markdown Source Below — copy into any .md file and export to PDF**

---

# **Trigonometric Integrals — Full Cheat Sheet (Study Notes Style)**

*Modern Notes Layout — A4 Friendly*

---

# 🟦 1. Basic Trigonometric Integrals

```
∫ sin x dx = -cos x + C  
∫ cos x dx = sin x + C  
∫ tan x dx = -ln|cos x| + C  
∫ cot x dx = ln|sin x| + C  
∫ sec x dx = ln|sec x + tan x| + C  
∫ csc x dx = ln|csc x - cot x| + C  
```

---

# 🟩 2. Powers of Sine & Cosine

### When integrating  ∫ sinᵐ(x) cosⁿ(x) dx

### ✔️ If **m is odd**

Take out one sine, convert rest:

```
sin²x = 1 – cos²x  
u = cos x  
```

### ✔️ If **n is odd**

Take out one cosine:

```
cos²x = 1 – sin²x  
u = sin x  
```

### ✔️ If both even

Use half-angle:

```
sin²x = (1 – cos 2x)/2  
cos²x = (1 + cos 2x)/2  
```

---

# 🟨 3. Powers of Secant & Tangent

### For ∫ secᵐ(x) tanⁿ(x) dx

### ✔️ If **n is odd**

Save sec x tan x

```
tan²x = sec²x – 1  
u = sec x  
```

### ✔️ If **m is even**

Save sec²x

```
sec²x = tan²x + 1  
u = tan x  
```

---

# 🟥 4. Product-to-Sum (Different Angle Products)

```
sin A sin B = ½[cos(A–B) – cos(A+B)]
cos A cos B = ½[cos(A–B) + cos(A+B)]
sin A cos B = ½[sin(A+B) + sin(A–B)]
```

---

# 🟪 5. Inverse Trigonometric Integrals

```
∫ dx / √(1 − x²)            = sin⁻¹ x + C  
∫ -dx / √(1 − x²)           = cos⁻¹ x + C  
∫ dx / (1 + x²)             = tan⁻¹ x + C  
∫ -dx / (1 + x²)            = cot⁻¹ x + C  
∫ dx / (|x|√(x² − 1))       = sec⁻¹ x + C  
∫ -dx / (|x|√(x² − 1))      = csc⁻¹ x + C  
```

---

# 🟧 6. Trig Substitution Guide

| Expression | Substitution | Identity          |
| ---------- | ------------ | ----------------- |
| √(a² − x²) | x = a sinθ   | 1 − sin²θ = cos²θ |
| √(a² + x²) | x = a tanθ   | 1 + tan²θ = sec²θ |
| √(x² − a²) | x = a secθ   | sec²θ − 1 = tan²θ |

---

# 🟦 7. Useful Identities

### Pythagorean

```
tan²x + 1 = sec²x  
1 + cot²x = csc²x  
```

### Double Angle

```
sin 2x = 2 sin x cos x  
cos 2x = cos²x – sin²x  
```

### Half Angle

```
sin²x = (1 − cos 2x)/2  
cos²x = (1 + cos 2x)/2  
```

---

# ⭐ 8. Patterns to Memorize Everything Faster

### ✔ Pattern 1: Sine–Cosine Cycle

sin → cos → –sin → –cos → repeat

### ✔ Pattern 2: Tan–Sec Pair

Because sec² = 1 + tan²

### ✔ Pattern 3: Powers Rule

Odd → save 1
Even → identity

### ✔ Pattern 4: Inverse Trig Patterns

* (1 – x²) → arcsin
* (1 + x²) → arctan
* (x² – 1) → arcsec

---

# END OF FULL VERSION

(Export this Markdown to PDF)

---

# 📄 **PDF 2 — CONDENSED VERSION (Style A: Classic Textbook)**

### **Markdown Source Below — One-Page Dense Sheet**

---

# **Trigonometric Integrals — Condensed Exam Sheet (A4)**

## BASIC INTEGRALS

```
∫ sin x dx = –cos x + C  
∫ cos x dx = sin x + C  
∫ tan x dx = –ln|cos x| + C  
∫ cot x dx = ln|sin x| + C  
∫ sec x dx = ln|sec x + tan x| + C  
∫ csc x dx = ln|csc x – cot x| + C  
```

## SEC & TAN

```
∫ sec²x dx = tan x + C  
∫ csc²x dx = –cot x + C  
∫ sec x tan x dx = sec x + C  
∫ csc x cot x dx = –csc x + C  
```

## INVERSE TRIG

```
∫ dx / √(1 − x²) = sin⁻¹x + C  
∫ dx / (1 + x²) = tan⁻¹x + C  
∫ dx / (|x|√(x²−1)) = sec⁻¹x + C  
```

## POWER REDUCTIONS

```
sin²x = (1 – cos 2x)/2  
cos²x = (1 + cos 2x)/2  
```

## PRODUCT-TO-SUM

```
sinA sinB = ½[cos(A–B) – cos(A+B)]  
cosA cosB = ½[cos(A–B) + cos(A+B)]  
sinA cosB = ½[sin(A+B) + sin(A–B)]  
```

## TRIG SUBS

```
√(a² – x²):   x = a sinθ  
√(a² + x²):   x = a tanθ  
√(x² – a²):   x = a secθ  
```

## KEY IDENTITIES

```
tan²x + 1 = sec²x  
cot²x + 1 = csc²x  
sin2x = 2sinx cosx  
cos2x = cos²x − sin²x  
```

---

# Done ✔

You now have:

### ✅ Modern full cheat sheet (Markdown, PDF-ready)

### ✅ Condensed classic sheet (Markdown, PDF-ready)

If you want:
📌 *A ZIP file* containing both
📌 Different styles
📌 Color-coded versions
📌 A version with examples
📌 An ultra-condensed “memory-only” sheet

Just tell me!
